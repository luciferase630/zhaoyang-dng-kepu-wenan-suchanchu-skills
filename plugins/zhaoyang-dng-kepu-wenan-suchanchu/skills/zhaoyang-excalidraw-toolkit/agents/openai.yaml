interface:
  display_name: "朝阳 Excalidraw 工具箱"
  short_description: "通过 MCP 或本地 REST API 操作 Excalidraw 画布。"
  default_prompt: "Use $zhaoyang-excalidraw-toolkit to inspect, edit, verify, and export an Excalidraw canvas."

dependencies:
  tools:
    - type: "mcp"
      value: "excalidraw"
      description: "Excalidraw MCP server connected to a local canvas server"
      transport: "stdio"
